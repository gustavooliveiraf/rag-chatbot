# Evaluation fixtures

`cases.json` holds the reference question set for `npm run evaluate` (FR-011). The
four cases here are illustrative placeholders — once the separate VTEX ingestion
project populates `documents`/`chunks`, replace `expected_source_titles` with the
actual page titles that should be cited for each answerable case.
